pool:
  name: MyAzurVM              # <-- your self-hosted pool name
  demands:
  - Agent.OS -equals Windows_NT

trigger:
- master                      # or main, match your branch

steps:
- script: echo "Hello from $(Agent.Name) on $(Agent.MachineName)"
  displayName: 'Smoke test'